using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class usingCinemachineTest : MonoBehaviour
{
    // Start is called before the first frame update
    //program functionality here so that the CameraManagerTest class 
    // can call these functions and assert if they fail.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
